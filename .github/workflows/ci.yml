name: CI/CD Pipeline for sammet

on:
  push:
    branches:
      - master
      - main

jobs:
  test:
    name: Run tests in built image
    runs-on: ubuntu-latest
    
    services:
      db:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: kubsu
          POSTGRES_PASSWORD: kubsu
          POSTGRES_DB: kubsu
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U kubsu -d kubsu"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t sammet-app:ci .

      - name: Run tests inside container
        run: |
          docker run --rm \
            --network host \
            -e DATABASE_URL=postgresql+psycopg://kubsu:kubsu@127.0.0.1:5432/kubsu \
            -v ${{ github.workspace }}/tests:/app/tests \
            sammet-app:ci \
            sh -c "pip install '.[test]' && python -m pytest tests -v"

  deploy:
    name: Deploy to server
    runs-on: self-hosted
    needs: test
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create network
        run: |
          podman network create sammet-network 2>/dev/null || true

      - name: Build image with Podman
        run: podman build -t localhost/sammet-app:latest .

      - name: Stop and remove old containers
        run: |
          podman rm -f sammet-app 2>/dev/null || true
          podman rm -f sammet-db 2>/dev/null || true

      - name: Start PostgreSQL container
        run: |
          podman run -d \
            --name sammet-db \
            --network sammet-network \
            --restart unless-stopped \
            -e POSTGRES_USER=kubsu \
            -e POSTGRES_PASSWORD=kubsu \
            -e POSTGRES_DB=kubsu \
            -v pgdata:/var/lib/postgresql/data \
            postgres:16-alpine

      - name: Wait for PostgreSQL
        run: |
          for i in $(seq 1 30); do
            podman exec sammet-db pg_isready -U kubsu -d kubsu && break
            echo "Waiting for PostgreSQL... ($i/30)"
            sleep 2
          done

      - name: Start application container
        run: |
          podman run -d \
            --name sammet-app \
            --network sammet-network \
            --restart unless-stopped \
            -p 8101:8101 \
            -e DATABASE_URL=postgresql+asyncpg://kubsu:kubsu@sammet-db:5432/kubsu \
            localhost/sammet-app:latest

      - name: Verify container is running
        run: |
          sleep 3
          podman ps --filter "name=sammet-app" --format "table {{.Names}}\t{{.Status}}"
          podman logs --tail 20 sammet-app